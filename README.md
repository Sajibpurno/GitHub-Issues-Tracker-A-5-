Assignment-5 : Github-issue-tracker


1. What is the difference between var, let, and const?
Ans- var was once used in js to declare variables, because of hoisting now it's not used anymore.
let is modern es6 variable declaration system which is reassignable.
const is modern es6 variable declaration system which is not reassignable.

2. What is the spread operator (...)?
Ans- spread operator  copies  elements from an array or object into another array or object.

3. What is the difference between map(), filter(), and forEach()?
Ans- map()– creates a new array by changing each item in the original array.
filter() – creates a new array with only the items that pass a condition.
forEach() – runs a function on each item and does not return an array.

4. What is an arrow function?
Ans- Arrow function is a short way to write a function in JavaScript using => sign.

5. What are template literals?
Ans- Template literals is a way to write strings in JavaScript using backticks  that allow to write multi-line text.


Username: admin
Password: admin123